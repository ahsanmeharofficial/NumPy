#if u want to marge the to data sets varticaly and horizently use staking function 
import numpy as np
arr1=np.array([10,20,30])
arr2=np.array([40,50,60])

print (np.vstack((arr1,arr2)))#v stack for vartical satking 
print (np.hstack((arr1,arr2))) #for horizental satkin