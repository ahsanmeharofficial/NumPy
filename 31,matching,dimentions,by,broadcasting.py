import numpy as np 
matrix = np.array([[1,2,3],[4,5,6]]) # this is D array 
vector=np.array ([10,20,30]) #use for brodcasting on arrary  () 1d array)
newarr = matrix + vector
print (newarr)

#this the brodcasting  is possbele when dimentions ar matched 

